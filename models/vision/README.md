# Vision Models

Vision is planned as small, offline-first utilities: QR, barcode, OCR, document edges, color detection, and small object detection where hardware permits.

The first device implementation should use static image analysis before live camera inference.
