# Preview Assets

Optional local preview assets can be placed here later.

The current preview does not require image assets. It draws the JARVIS orb with tkinter so the preview works with Python standard library only.
